
// function definition
fn hello() { // args are seperated by ;
    int a = 5;
    // output 95 + the input arg
    putchar(a + 95);
}

hello();
hello();
hello();
hello();

putchar(10);

exit(0); // exit code 0, success

